<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<select id="selectAll" resultType="com.hb.model.UserVo">
		SELECT id, password, name, post, main_addr, sub_addr, sex, phone, mobile, email, point, pm FROM TB_USER, TB_PM where TB_USER.pm_num=TB_PM.pm_num
  	</select>
	<select id="selectOne" resultType="com.hb.model.UserVo" parameterType="String">
		SELECT id, `name`, post, main_addr, sub_addr, sex, phone, mobile, email, `point`, pm FROM TB_USER, TB_PM where TB_USER.pm_num=TB_PM.pm_num and TB_USER.id=#{id}
	</select>
</mapper>






